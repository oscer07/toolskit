<UserControl x:Class="ToolkitApp.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgDarkBrush}">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="About Tool Kit" Foreground="White" FontSize="36" FontWeight="Light" HorizontalAlignment="Center"/>
            <TextBlock Text="Version 2.0 (Modern UI)" Foreground="{StaticResource TextLightBrush}" FontSize="18" HorizontalAlignment="Center" Margin="0,10,0,0"/>
            <TextBlock Text="Author: oscer" Foreground="{StaticResource AccentBrush}" FontSize="24" HorizontalAlignment="Center" Margin="0,20,0,0"/>
            
            <TextBlock Text="A unified, slick interface for executing command-line tools." 
                       Foreground="{StaticResource TextLightBrush}" FontSize="16" HorizontalAlignment="Center" Margin="0,40,0,0"/>
            <TextBlock Text="Supports CMD and PowerShell with output capture and Admin execution." 
                       Foreground="{StaticResource TextLightBrush}" FontSize="16" HorizontalAlignment="Center" Margin="0,5,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
