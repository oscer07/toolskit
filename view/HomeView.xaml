<UserControl x:Class="ToolkitApp.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgDarkBrush}">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Welcome to Tool Kit" Foreground="White" FontSize="48" FontWeight="Light" HorizontalAlignment="Center"/>
            <TextBlock Text="Author: oscer" Foreground="{StaticResource TextLightBrush}" FontSize="24" HorizontalAlignment="Center" Margin="0,10,0,0"/>
            <TextBlock Text="Navigate to the Tools Workspace to get started." Foreground="{StaticResource AccentBrush}" FontSize="16" HorizontalAlignment="Center" Margin="0,30,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
